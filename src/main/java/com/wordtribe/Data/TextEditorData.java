package com.wordtribe.Data;

import com.wordtribe.Data.PathHandlers.TimedPath;
import javafx.collections.ObservableList;


// Singleton class
public class TextEditorData {
    private String text;
    private ObservableList<TimedPath> lastOpened;
    private TimedPath currentPath;

    // Load text file from disk
    // Save text file to disk
    // Rename currentPath
}
